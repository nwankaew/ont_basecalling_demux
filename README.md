# bacterial_basecalling_demux
Dorado basecalling pipeline for Nanopore sequencing data. The sequencers are basecall by Dorado and demultiplexed. The the file were rename into common name follow by column alias of sampleSheet.csv  (sample.fastq.gz). Then , the final fastq file were move to in project_id (from column "sample_id").
